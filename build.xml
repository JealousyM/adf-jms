<?xml version="1.0" encoding="windows-1251" ?>
<!--Ant buildfile generated by Oracle JDeveloper-->
<!--Generated Feb 27, 2016 5:59:46 AM-->
<project xmlns="antlib:org.apache.tools.ant" name="JMSADFApp" basedir=".">
  <property file="build.properties"/>
  <target name="build" description="Deploy JDeveloper profiles">
    <echo>build application</echo>
    <echo>${basedir}</echo>
    <taskdef name="ojdeploy" classname="oracle.jdeveloper.deploy.ant.OJDeployAntTask" uri="oraclelib:OJDeployAntTask"
             classpath="${ant.library}"/>
    <ora:ojdeploy xmlns:ora="oraclelib:OJDeployAntTask" executable="${ojdeploy.path}"
                  ora:buildscript="${basedir}/deploy/ojdeploy-build.xml"
                  ora:statuslog="${basedir}/deploy/ojdeploy-statuslog.xml">
      <ora:deploy>
        <ora:parameter name="workspace" value="${basedir}/${workspace.path}"/>
        <ora:parameter name="profile" value="${profile.name}"/>
        <ora:parameter name="nocompile" value="false"/>
        <ora:parameter name="outputfile" value="${outputfile}"/>
      </ora:deploy>
    </ora:ojdeploy>
  </target>
<taskdef name="wldeploy" classname="weblogic.ant.taskdefs.management.WLDeploy">
<classpath>
<pathelement location="${wl.path}"/>
</classpath> 
</taskdef> 
<target name="deploy" depends="build">
<echo>Deploy the ear file</echo>
    <wldeploy
      action="deploy" library="false" remote="true" upload="true"
      name="${application.name}" source="${deploy.dir}/${outputfile}"
      user="${wl.user}" password="${wl.password}"
      adminurl="${wl.host}" targets="${wl.target}" usenonexclusivelock="true"/>
  </target>
</project>